info:
  name: route-post
  type: http

http:
  method: POST
  url: https://christina.pathway4.click/routes/
  auth: inherit
  body:
    mode: json
    json: |
      {
        "date": "2026-05-14",
        "service_zone": "North District",
        "driver_id": 1
      }

headers:
  Content-Type: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
